{
  "name": "data-tier",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create-local-db": "cross-env DB_ENDPOINT=http://localhost:8000 DB_REGION=eu-central-1 NODE_ENV=development node createContactsTable.js"
  },
  "dependencies": {
    "aws-sdk": "^2.409.0",
    "cross-env": "^5.2.0"
  }
}
